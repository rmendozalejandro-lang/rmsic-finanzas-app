const fs = require('fs');
const path = require('path');

const target = path.join(process.cwd(), 'app', '(private)', 'bancos', 'conciliacion', 'page.tsx');

if (!fs.existsSync(target)) {
  console.error('No se encontro el archivo:', target);
  process.exit(1);
}

let text = fs.readFileSync(target, 'utf8');

if (text.includes('showConciliarParcialForm') || text.includes('conciliar_pago_parcial_bancario')) {
  console.log('El archivo ya parece tener conciliacion parcial. No se aplicaron cambios.');
  process.exit(0);
}

function replaceOnce(label, needle, replacement) {
  if (!text.includes(needle)) {
    console.error(`No se encontro el bloque para aplicar: ${label}`);
    console.error('No se modifico el archivo. Revisa que sea la version esperada de app/(private)/bancos/conciliacion/page.tsx');
    process.exit(1);
  }
  text = text.replace(needle, replacement);
  console.log('OK:', label);
}


replaceOnce("type MovimientoParcial", "type MovimientoManual = {\n  id: string\n  fecha: string | null\n  tipo_movimiento: 'ingreso' | 'egreso' | string\n  tipo_documento: string | null\n  numero_documento: string | null\n  descripcion: string | null\n  monto_neto: number | string | null\n  monto_iva: number | string | null\n  monto_exento: number | string | null\n  impuesto_especifico: number | string | null\n  monto_total: number | string\n  estado: string\n  clientes?: RelacionNombre | RelacionNombre[] | null\n  proveedores?: RelacionNombre | RelacionNombre[] | null\n  categorias?: CategoriaMovimiento | CategoriaMovimiento[] | null\n}\n\n", "type MovimientoManual = {\n  id: string\n  fecha: string | null\n  tipo_movimiento: 'ingreso' | 'egreso' | string\n  tipo_documento: string | null\n  numero_documento: string | null\n  descripcion: string | null\n  monto_neto: number | string | null\n  monto_iva: number | string | null\n  monto_exento: number | string | null\n  impuesto_especifico: number | string | null\n  monto_total: number | string\n  estado: string\n  clientes?: RelacionNombre | RelacionNombre[] | null\n  proveedores?: RelacionNombre | RelacionNombre[] | null\n  categorias?: CategoriaMovimiento | CategoriaMovimiento[] | null\n}\n\ntype MovimientoParcial = MovimientoManual & {\n  total_documento: number | string\n  total_pagado_parcial: number | string\n  saldo_pendiente: number | string\n  cantidad_pagos: number | string\n  estado_pago_calculado: string\n}\n\n");

replaceOnce("estados de conciliacion parcial", "  const [manualLoading, setManualLoading] = useState(false)\n  const [manualSaving, setManualSaving] = useState(false)\n  const [manualError, setManualError] = useState('')\n\n", "  const [manualLoading, setManualLoading] = useState(false)\n  const [manualSaving, setManualSaving] = useState(false)\n  const [manualError, setManualError] = useState('')\n\n  const [showConciliarParcialForm, setShowConciliarParcialForm] =\n    useState(false)\n  const [parcialFila, setParcialFila] = useState<FilaBanco | null>(null)\n  const [parcialBusqueda, setParcialBusqueda] = useState('')\n  const [parcialTipo, setParcialTipo] = useState<\n    'todos' | 'ingreso' | 'egreso'\n  >('todos')\n  const [parcialResultados, setParcialResultados] = useState<\n    MovimientoParcial[]\n  >([])\n  const [parcialMovimientoId, setParcialMovimientoId] = useState('')\n  const [parcialLoading, setParcialLoading] = useState(false)\n  const [parcialSaving, setParcialSaving] = useState(false)\n  const [parcialError, setParcialError] = useState('')\n\n");

replaceOnce("funciones de conciliacion parcial", "  const cargarMovimientosMultiple = async (\n", "  const cargarMovimientosParcial = async (\n    fila: FilaBanco | null = parcialFila,\n    busqueda: string = parcialBusqueda,\n    tipo: 'todos' | 'ingreso' | 'egreso' = parcialTipo\n  ) => {\n    if (!empresaActivaId || !fila) return\n\n    setParcialLoading(true)\n    setParcialError('')\n    setParcialMovimientoId('')\n\n    const texto = busqueda.trim().toLowerCase()\n    const montoFila = getMontoFila(fila)\n\n    let query = supabase\n      .from('movimientos')\n      .select(`\n        id,\n        fecha,\n        tipo_movimiento,\n        tipo_documento,\n        numero_documento,\n        descripcion,\n        monto_neto,\n        monto_iva,\n        monto_exento,\n        impuesto_especifico,\n        monto_total,\n        estado,\n        clientes:cliente_id (\n          nombre,\n          rut\n        ),\n        proveedores:proveedor_id (\n          nombre,\n          rut\n        ),\n        categorias:categoria_id (\n          nombre\n        )\n      `)\n      .eq('empresa_id', empresaActivaId)\n      .is('deleted_at', null)\n      .order('fecha', { ascending: false })\n      .limit(300)\n\n    if (tipo !== 'todos') {\n      query = query.eq('tipo_movimiento', tipo)\n    }\n\n    const { data, error: queryError } = await query\n\n    if (queryError) {\n      setParcialError(`Error al buscar movimientos: ${queryError.message}`)\n      setParcialResultados([])\n      setParcialLoading(false)\n      return\n    }\n\n    const baseResultados = ((data ?? []) as unknown as MovimientoManual[]).filter(\n      (movimiento) => {\n        if (!texto) return true\n\n        const campos = [\n          movimiento.tipo_movimiento,\n          movimiento.tipo_documento,\n          movimiento.numero_documento,\n          movimiento.descripcion,\n          movimiento.estado,\n          movimiento.monto_total,\n          movimiento.monto_neto,\n          movimiento.monto_iva,\n          movimiento.monto_exento,\n          movimiento.impuesto_especifico,\n          getRelacionNombre(movimiento.clientes),\n          getRelacionNombre(movimiento.proveedores),\n          getRelacionNombre(movimiento.categorias),\n        ]\n          .filter(Boolean)\n          .join(' ')\n          .toLowerCase()\n\n        return campos.includes(texto)\n      }\n    )\n\n    const ids = baseResultados.map((movimiento) => movimiento.id)\n    const resumenPorMovimiento = new Map<\n      string,\n      {\n        total_documento: number | string\n        total_pagado_parcial: number | string\n        saldo_pendiente: number | string\n        cantidad_pagos: number | string\n        estado_pago_calculado: string\n      }\n    >()\n\n    if (ids.length > 0) {\n      const { data: resumenData, error: resumenError } = await supabase\n        .from('v_conciliacion_pago_parcial_resumen')\n        .select(\n          'movimiento_id,total_documento,total_pagado_parcial,saldo_pendiente,cantidad_pagos,estado_pago_calculado'\n        )\n        .in('movimiento_id', ids)\n\n      if (resumenError) {\n        setParcialError(\n          `Error al cargar saldos parciales: ${resumenError.message}`\n        )\n        setParcialResultados([])\n        setParcialLoading(false)\n        return\n      }\n\n      ;(resumenData ?? []).forEach((item: any) => {\n        resumenPorMovimiento.set(String(item.movimiento_id), {\n          total_documento: item.total_documento,\n          total_pagado_parcial: item.total_pagado_parcial,\n          saldo_pendiente: item.saldo_pendiente,\n          cantidad_pagos: item.cantidad_pagos,\n          estado_pago_calculado: item.estado_pago_calculado,\n        })\n      })\n    }\n\n    let resultados: MovimientoParcial[] = baseResultados\n      .map((movimiento) => {\n        const resumen = resumenPorMovimiento.get(movimiento.id)\n\n        return {\n          ...movimiento,\n          total_documento: resumen?.total_documento ?? movimiento.monto_total,\n          total_pagado_parcial: resumen?.total_pagado_parcial ?? 0,\n          saldo_pendiente: resumen?.saldo_pendiente ?? movimiento.monto_total,\n          cantidad_pagos: resumen?.cantidad_pagos ?? 0,\n          estado_pago_calculado: resumen?.estado_pago_calculado ?? 'sin_pagos',\n        }\n      })\n      .filter((movimiento) => Number(movimiento.saldo_pendiente ?? 0) > 0.49)\n\n    resultados = resultados.sort((a, b) => {\n      const diffA = Math.abs(Number(a.saldo_pendiente ?? 0) - montoFila)\n      const diffB = Math.abs(Number(b.saldo_pendiente ?? 0) - montoFila)\n\n      if (diffA !== diffB) return diffA - diffB\n\n      return String(b.fecha || '').localeCompare(String(a.fecha || ''))\n    })\n\n    setParcialResultados(resultados)\n    setParcialMovimientoId(resultados[0]?.id || '')\n    setParcialLoading(false)\n  }\n\n  const abrirConciliacionParcial = async (fila: FilaBanco) => {\n    const tipo = Number(fila.cargo ?? 0) > 0 ? 'egreso' : 'ingreso'\n\n    setParcialFila(fila)\n    setParcialBusqueda('')\n    setParcialTipo(tipo)\n    setParcialResultados([])\n    setParcialMovimientoId('')\n    setParcialError('')\n    setShowConciliarParcialForm(true)\n\n    await cargarMovimientosParcial(fila, '', tipo)\n  }\n\n  const cerrarConciliacionParcial = () => {\n    if (parcialSaving) return\n\n    setShowConciliarParcialForm(false)\n    setParcialFila(null)\n    setParcialBusqueda('')\n    setParcialTipo('todos')\n    setParcialResultados([])\n    setParcialMovimientoId('')\n    setParcialError('')\n  }\n\n  const getMovimientoParcialSeleccionado = () => {\n    return parcialResultados.find(\n      (movimiento) => movimiento.id === parcialMovimientoId\n    )\n  }\n\n  const conciliarParcial = async () => {\n    if (!parcialFila) {\n      setParcialError('No se encontr\u00f3 la l\u00ednea de cartola seleccionada.')\n      return\n    }\n\n    const movimiento = getMovimientoParcialSeleccionado()\n\n    if (!movimiento) {\n      setParcialError('Debes seleccionar una factura o movimiento.')\n      return\n    }\n\n    const montoBanco = getMontoFila(parcialFila)\n    const saldoPendiente = Number(movimiento.saldo_pendiente ?? 0)\n\n    if (montoBanco > saldoPendiente + 0.49) {\n      setParcialError(\n        `El pago excede el saldo pendiente. Monto cartola: ${formatCLP(\n          montoBanco\n        )}. Saldo pendiente: ${formatCLP(saldoPendiente)}.`\n      )\n      return\n    }\n\n    const confirmar = window.confirm(\n      `\u00bfAplicar pago parcial por ${formatCLP(\n        montoBanco\n      )} al movimiento ${movimiento.numero_documento || movimiento.descripcion || ''}?`\n    )\n\n    if (!confirmar) return\n\n    setParcialSaving(true)\n    setParcialError('')\n    setError('')\n    setSuccess('')\n\n    const { data, error: rpcError } = await supabase.rpc(\n      'conciliar_pago_parcial_bancario',\n      {\n        p_banco_importacion_fila_id: parcialFila.id,\n        p_movimiento_id: movimiento.id,\n        p_observacion: `Conciliaci\u00f3n parcial desde app: ${\n          movimiento.tipo_movimiento\n        } ${movimiento.tipo_documento || ''} ${\n          movimiento.numero_documento || ''\n        }`.trim(),\n      }\n    )\n\n    if (rpcError) {\n      setParcialError(rpcError.message)\n      setParcialSaving(false)\n      return\n    }\n\n    const resultado = Array.isArray(data)\n      ? (data[0] as { saldo_pendiente?: number | string } | undefined)\n      : null\n    const saldo = resultado?.saldo_pendiente\n\n    setSuccess(\n      saldo !== undefined && Number(saldo) > 0.49\n        ? `Pago parcial registrado correctamente. Saldo pendiente: ${formatCLP(\n            saldo\n          )}.`\n        : 'Pago parcial registrado correctamente. La factura qued\u00f3 pagada.'\n    )\n    await cargarDatos()\n\n    setParcialSaving(false)\n    cerrarConciliacionParcial()\n  }\n\n  const cargarMovimientosMultiple = async (\n");

replaceOnce("boton Conciliar parcial", "                      <button\n                        type=\"button\"\n                        onClick={() => abrirConciliacionMultiple(fila)}\n                        disabled={processing}\n                        className=\"w-full rounded-lg border border-purple-200 bg-purple-50 px-3 py-1.5 text-xs font-medium text-purple-700 hover:bg-purple-100 disabled:opacity-60\"\n                      >\n                        Conciliar m\u00faltiple\n                      </button>\n", "                      <button\n                        type=\"button\"\n                        onClick={() => abrirConciliacionParcial(fila)}\n                        disabled={processing}\n                        className=\"w-full rounded-lg border border-amber-200 bg-amber-50 px-3 py-1.5 text-xs font-medium text-amber-700 hover:bg-amber-100 disabled:opacity-60\"\n                      >\n                        Conciliar parcial\n                      </button>\n\n                      <button\n                        type=\"button\"\n                        onClick={() => abrirConciliacionMultiple(fila)}\n                        disabled={processing}\n                        className=\"w-full rounded-lg border border-purple-200 bg-purple-50 px-3 py-1.5 text-xs font-medium text-purple-700 hover:bg-purple-100 disabled:opacity-60\"\n                      >\n                        Conciliar m\u00faltiple\n                      </button>\n");

replaceOnce("modal Conciliacion parcial", "      {showConciliarMultipleForm ? (\n", "      {showConciliarParcialForm ? (\n        <div className=\"fixed inset-0 z-50 flex items-center justify-center bg-slate-900/40 p-4\">\n          <div className=\"max-h-[90vh] w-full max-w-6xl overflow-y-auto rounded-3xl bg-white p-6 shadow-xl\">\n            <div className=\"flex flex-col gap-3 border-b pb-4 md:flex-row md:items-start md:justify-between\">\n              <div>\n                <p className=\"text-sm font-medium text-slate-500\">\n                  Conciliar varios pagos contra una factura\n                </p>\n\n                <h2 className=\"mt-1 text-xl font-semibold text-slate-900\">\n                  Conciliaci\u00f3n parcial\n                </h2>\n\n                <p className=\"mt-1 text-sm text-slate-600\">\n                  {parcialFila?.descripcion_original || 'Sin descripci\u00f3n'} \u00b7{' '}\n                  {formatDate(parcialFila?.fecha)} \u00b7{' '}\n                  {formatCLP(getMontoFila(parcialFila))}\n                </p>\n              </div>\n\n              <button\n                type=\"button\"\n                onClick={cerrarConciliacionParcial}\n                disabled={parcialSaving}\n                className=\"rounded-xl border border-slate-300 px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50 disabled:opacity-60\"\n              >\n                Cerrar\n              </button>\n            </div>\n\n            <div className=\"mt-5 space-y-5\">\n              {parcialError ? (\n                <div className=\"rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700\">\n                  {parcialError}\n                </div>\n              ) : null}\n\n              <div className=\"grid gap-4 md:grid-cols-[1fr_220px_auto]\">\n                <div>\n                  <label className=\"mb-1 block text-sm font-medium text-slate-700\">\n                    Buscar factura o movimiento existente\n                  </label>\n                  <input\n                    type=\"text\"\n                    value={parcialBusqueda}\n                    onChange={(event) => setParcialBusqueda(event.target.value)}\n                    onKeyDown={(event) => {\n                      if (event.key === 'Enter') {\n                        event.preventDefault()\n                        void cargarMovimientosParcial()\n                      }\n                    }}\n                    placeholder=\"N\u00famero de factura, proveedor/cliente, descripci\u00f3n o total factura\"\n                    className=\"w-full rounded-xl border border-slate-300 px-3 py-2 text-sm outline-none focus:border-slate-900\"\n                  />\n                </div>\n\n                <div>\n                  <label className=\"mb-1 block text-sm font-medium text-slate-700\">\n                    Tipo\n                  </label>\n                  <select\n                    value={parcialTipo}\n                    onChange={(event) =>\n                      setParcialTipo(\n                        event.target.value as 'todos' | 'ingreso' | 'egreso'\n                      )\n                    }\n                    className=\"w-full rounded-xl border border-slate-300 px-3 py-2 text-sm outline-none focus:border-slate-900\"\n                  >\n                    <option value=\"todos\">Todos</option>\n                    <option value=\"ingreso\">Ingresos</option>\n                    <option value=\"egreso\">Egresos</option>\n                  </select>\n                </div>\n\n                <div className=\"flex items-end\">\n                  <button\n                    type=\"button\"\n                    onClick={() => cargarMovimientosParcial()}\n                    disabled={parcialLoading || parcialSaving}\n                    className=\"w-full rounded-xl bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-800 disabled:opacity-60\"\n                  >\n                    {parcialLoading ? 'Buscando...' : 'Buscar'}\n                  </button>\n                </div>\n              </div>\n\n              <div className=\"grid gap-4 md:grid-cols-4\">\n                <div className=\"rounded-2xl border bg-slate-50 p-4\">\n                  <p className=\"text-sm text-slate-500\">Monto cartola</p>\n                  <p className=\"mt-1 text-lg font-semibold text-slate-900\">\n                    {formatCLP(getMontoFila(parcialFila))}\n                  </p>\n                </div>\n\n                <div className=\"rounded-2xl border bg-slate-50 p-4\">\n                  <p className=\"text-sm text-slate-500\">Total documento</p>\n                  <p className=\"mt-1 text-lg font-semibold text-slate-900\">\n                    {formatCLP(getMovimientoParcialSeleccionado()?.total_documento)}\n                  </p>\n                </div>\n\n                <div className=\"rounded-2xl border bg-slate-50 p-4\">\n                  <p className=\"text-sm text-slate-500\">Ya pagado</p>\n                  <p className=\"mt-1 text-lg font-semibold text-slate-900\">\n                    {formatCLP(\n                      getMovimientoParcialSeleccionado()?.total_pagado_parcial\n                    )}\n                  </p>\n                </div>\n\n                <div\n                  className={\n                    Number(\n                      getMovimientoParcialSeleccionado()?.saldo_pendiente ?? 0\n                    ) >= getMontoFila(parcialFila)\n                      ? 'rounded-2xl border border-emerald-200 bg-emerald-50 p-4'\n                      : 'rounded-2xl border border-red-200 bg-red-50 p-4'\n                  }\n                >\n                  <p className=\"text-sm text-slate-500\">Saldo pendiente</p>\n                  <p className=\"mt-1 text-lg font-semibold text-slate-900\">\n                    {formatCLP(\n                      getMovimientoParcialSeleccionado()?.saldo_pendiente\n                    )}\n                  </p>\n                </div>\n              </div>\n\n              <div className=\"rounded-2xl border bg-amber-50 p-4 text-sm text-amber-800\">\n                Usa esta opci\u00f3n cuando una factura o movimiento ya creado se paga\n                con dos o m\u00e1s l\u00edneas bancarias, incluso desde distintas cuentas.\n                Cada l\u00ednea queda conciliada como pago parcial y el saldo se\n                actualiza autom\u00e1ticamente.\n              </div>\n\n              <div className=\"overflow-hidden rounded-xl border text-sm\">\n                <div className=\"grid grid-cols-8 bg-slate-50 text-xs uppercase text-slate-500\">\n                  <div className=\"px-4 py-3 font-semibold\">Sel.</div>\n                  <div className=\"px-4 py-3 font-semibold\">Fecha</div>\n                  <div className=\"px-4 py-3 font-semibold\">Tipo</div>\n                  <div className=\"px-4 py-3 font-semibold\">Documento</div>\n                  <div className=\"px-4 py-3 font-semibold\">Tercero</div>\n                  <div className=\"px-4 py-3 font-semibold\">Descripci\u00f3n</div>\n                  <div className=\"px-4 py-3 font-semibold\">Total</div>\n                  <div className=\"px-4 py-3 font-semibold\">Saldo</div>\n                </div>\n\n                {parcialResultados.length > 0 ? (\n                  <div className=\"divide-y\">\n                    {parcialResultados.map((movimiento) => {\n                      const tercero =\n                        getRelacionNombre(movimiento.clientes) ||\n                        getRelacionNombre(movimiento.proveedores) ||\n                        '-'\n                      const categoria = getRelacionNombre(movimiento.categorias)\n                      const seleccionado = movimiento.id === parcialMovimientoId\n                      const montoBanco = getMontoFila(parcialFila)\n                      const saldoPendiente = Number(\n                        movimiento.saldo_pendiente ?? 0\n                      )\n                      const excedeSaldo = montoBanco > saldoPendiente + 0.49\n\n                      return (\n                        <label\n                          key={movimiento.id}\n                          className={\n                            seleccionado\n                              ? 'grid cursor-pointer grid-cols-8 items-center bg-amber-50'\n                              : 'grid cursor-pointer grid-cols-8 items-center hover:bg-slate-50'\n                          }\n                        >\n                          <div className=\"px-4 py-3\">\n                            <input\n                              type=\"radio\"\n                              name=\"movimiento-parcial\"\n                              checked={seleccionado}\n                              onChange={() =>\n                                setParcialMovimientoId(movimiento.id)\n                              }\n                            />\n                          </div>\n\n                          <div className=\"px-4 py-3\">\n                            {formatDate(movimiento.fecha)}\n                          </div>\n\n                          <div className=\"px-4 py-3 capitalize\">\n                            {movimiento.tipo_movimiento}\n                            <p className=\"text-xs text-slate-500\">\n                              {movimiento.estado_pago_calculado}\n                            </p>\n                          </div>\n\n                          <div className=\"px-4 py-3\">\n                            <p className=\"font-medium text-slate-900\">\n                              {movimiento.tipo_documento || '-'}\n                            </p>\n                            <p className=\"text-xs text-slate-500\">\n                              {movimiento.numero_documento || 'Sin n\u00famero'}\n                            </p>\n                          </div>\n\n                          <div className=\"px-4 py-3\">\n                            <p className=\"font-medium text-slate-900\">\n                              {tercero}\n                            </p>\n                            {categoria ? (\n                              <p className=\"text-xs text-slate-500\">\n                                {categoria}\n                              </p>\n                            ) : null}\n                          </div>\n\n                          <div className=\"px-4 py-3 text-slate-700\">\n                            {movimiento.descripcion || '-'}\n                            {Number(movimiento.total_pagado_parcial ?? 0) > 0 ? (\n                              <p className=\"mt-1 text-xs font-medium text-amber-700\">\n                                Ya pagado:{' '}\n                                {formatCLP(movimiento.total_pagado_parcial)}\n                              </p>\n                            ) : null}\n                            {excedeSaldo ? (\n                              <p className=\"mt-1 text-xs font-medium text-red-700\">\n                                Esta l\u00ednea excede el saldo pendiente.\n                              </p>\n                            ) : null}\n                          </div>\n\n                          <div className=\"px-4 py-3 font-semibold text-slate-900\">\n                            {formatCLP(movimiento.total_documento)}\n                          </div>\n\n                          <div className=\"px-4 py-3 font-semibold text-amber-700\">\n                            {formatCLP(movimiento.saldo_pendiente)}\n                          </div>\n                        </label>\n                      )\n                    })}\n                  </div>\n                ) : (\n                  <div className=\"px-4 py-5 text-center text-slate-500\">\n                    No hay movimientos con saldo pendiente para la b\u00fasqueda\n                    actual. Prueba con n\u00famero de factura, proveedor/cliente,\n                    descripci\u00f3n o total del documento.\n                  </div>\n                )}\n              </div>\n\n              <div className=\"flex flex-col-reverse gap-3 border-t pt-4 sm:flex-row sm:justify-end\">\n                <button\n                  type=\"button\"\n                  onClick={cerrarConciliacionParcial}\n                  disabled={parcialSaving}\n                  className=\"rounded-xl border border-slate-300 px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50 disabled:opacity-60\"\n                >\n                  Cancelar\n                </button>\n\n                <button\n                  type=\"button\"\n                  onClick={conciliarParcial}\n                  disabled={\n                    parcialSaving ||\n                    !parcialMovimientoId ||\n                    getMontoFila(parcialFila) >\n                      Number(\n                        getMovimientoParcialSeleccionado()?.saldo_pendiente ?? 0\n                      ) +\n                        0.49\n                  }\n                  className=\"rounded-xl bg-amber-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-amber-700 disabled:opacity-60\"\n                >\n                  {parcialSaving ? 'Conciliando...' : 'Aplicar pago parcial'}\n                </button>\n              </div>\n            </div>\n          </div>\n        </div>\n      ) : null}\n\n      {showConciliarMultipleForm ? (\n");

fs.writeFileSync(target, text, 'utf8');
console.log('Listo. Archivo actualizado:', target);
